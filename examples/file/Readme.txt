This example configures a FileAgent that writes the entire changelog to files. The files will be created in the 'files' folder.

Required configuration:
 Open config/Daemon.properties and change
   url=<the url to the changelog>
   username=<The user name to use when accessing the change log>
   password=<The password to use when accessing the change log>

Usage:
 ../../changelog.sh

